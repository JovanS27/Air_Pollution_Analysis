MY_QUERY = """

DROP TABLE IF EXISTS GP;

CREATE TABLE GP (
    GP_ID INT PRIMARY KEY,
    GP_PRACTICE VARCHAR(50),
    LATITUDE NUMERIC,
    LONGITUDE NUMERIC,
	PERCENTAGE DECIMAL,
    aqi DECIMAL,
	co DECIMAL,
	no DECIMAL,
	no2 DECIMAL,
	o3 DECIMAL,
	so2 DECIMAL,
	pm2_5 DECIMAL,
	pm10 DECIMAL,
	nh3 DECIMAL
);



"""